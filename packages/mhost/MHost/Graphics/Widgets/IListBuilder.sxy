(using Sys.Maths)

(using MHost)
(using MHost.Graphics)
(using MHost.Graphics.Widgets)

(interface $.IListBuilder
	(extends $.IControl)
	(SetRect (Rectf rect)(IGui g) -> )
	(AddRow (IListRow row) -> )
)
