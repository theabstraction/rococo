(interface 
	(as.sxy Rococo.ECS.IECS "ecs")
	(as.cpp Rococo.IECSBase)
	
	(context factory Rococo.IECS)
	(methods
        (Deprecate (ROID roid) -> (Bool success))
        (DeprecateAll ->)
		(IsActive (ROID roid)-> (Bool isActive))
		(MaxTableEntries -> (Int64 numberOfEntries))
		(NewROID -> (ROID roid))
	)
	
	(factory Rococo.ECS.GetECS)
)
