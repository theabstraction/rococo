// MPlat initialization file
// This will be run before MPlatform's graphics and geometry objects are initialized
// Since much of MPlat is not initialized at this point, we are restricted to the MPlatOpts functions
// See rococo.scripts.cpp for a list of what can be defined

(namespace EntryPoint)
	(alias Main EntryPoint.Main)
	
(using MPlat)

(function Main (Int32 id)->(Int32 exitCode):
	(MPlat.SetMaxEntities 1000)
)